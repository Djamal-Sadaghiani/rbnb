<%= @location %>
<% @parking_spaces.each do |parking_space| %>
<br>
<%= parking_space.address %>
<% end %>

<div class="parking-space-map-show">
  <div id="map_search" style="height: 80vh; width: 50vw" data-markers="<%= @markers.to_json %>"></div>
</div>
